server:
  port: 8081
spring:
  application:
    name: lottery-admin
  main:
    allow-bean-definition-overriding: true
eureka:
  client:
    service-url:
      defaultZone: http://lottery-eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true

apiName: lottery-api-c1